# index.html is deployed as a real static file (no longer excluded) — only
# /api/* is routed through api/gateway.js (see vercel.json), so the static
# UI is served directly by Vercel and never depends on the function's
# filesystem/bundling behavior.

# Local-only dev artifacts — never needed by the deployed function.
test/
.env.local
.env.example
