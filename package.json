{
  "name": "blog-automation",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Blog Automation - Phase 1: Secure Claude draft generation",
  "scripts": {
    "dev": "node --env-file-if-exists=.env.local server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.120.0",
    "zod": "^4.4.3"
  }
}
